import { ImporeSortPipe } from './impore-sort.pipe';

describe('ImporeSortPipe', () => {
  it('create an instance', () => {
    const pipe = new ImporeSortPipe();
    expect(pipe).toBeTruthy();
  });
});
